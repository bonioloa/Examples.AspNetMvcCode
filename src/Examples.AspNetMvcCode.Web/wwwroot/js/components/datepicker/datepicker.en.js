/*dependent on
 * materialize
 * immutable
 */

//translation not part of materialize library
//for now we force universal date format 
//for eng we just use default i18n values

var ConfigDatePicker = Object.freeze({
    firstDay: 1,
    format: 'yyyy-mm-dd',
    showClearBtn: true,
});