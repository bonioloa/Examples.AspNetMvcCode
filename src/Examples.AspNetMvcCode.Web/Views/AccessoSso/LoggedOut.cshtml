@*@model *@

@inject IMainLocalizer _localizer

@{
    Layout = MvcComponents.TemplatePanelPageFile;

    ViewData.SetTitle(_localizer[nameof(LocalizedStr.SsoLoggedOutTitle)]);
    ViewData.SetLogo(InfoAndLogo.CentralPanelLogoProduct);//use product, no info on tenant available here
    ViewData.SetShowLanguageSelector(false);//page can't be reloaded
}
@section styles
{   
}
@section scripts
{
}

@* come da suggerimento cliente (ADR) questa pagina non deve rimandare alla pagina di immissione token.
    Così il cliente è forzato a chiudere la scheda o riaccedere usando il link con token già compreso *@
<div class="row row-line-x2-before row-line-after">
    <div class="col xl12 l12 m12 s12 center-align">
        <h5 class="simple-panel-title ">
           @_localizer[nameof(LocalizedStr.SsoLoggedOutTitle)]
        </h5> 
    </div>
</div>